import { Message } from "discord.js";

module.exports = {
    name: "messageCreate",
    async execute(message: Message) {
        try {
            
        } catch (error) {
            console.error(error);
        }
    }
}